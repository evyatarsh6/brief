# PRD.md

> location: `docs/PRD.md`  
> Objective: To define what is being built, for whom, why, and what is considered success.

---

## 1. Background

`<Describe the business/technical background. Why is the project needed? What pain does it solve? What is the current situation?>`

---

## 2. Problem Statement

Business problem:
`<What is the business problem?>`

Technical problem:
`<What is the technical problem?>`

Current pain points:
- `<Pain point 1>`
- `<Pain point 2>`
- `<Pain point 3>`

---

## 3. Goals

Primary goals:
- `<Goal 1>`
- `<Goal 2>`
- `<Goal 3>`

Success metrics:
- `<Metric 1>`
- `<Metric 2>`
- `<Metric 3>`

---

## 4. Non-Goals

This project will not include:
- `<Non-goal 1>`
- `<Non-goal 2>`
- `<Non-goal 3>`

---

## 5. Users / Personas

### User 1: `<Name / Role>`
Description:
`<Who is the user and what does he need?>`

Main needs:
- `<Need 1>`
- `<Need 2>`

### User 2: `<Name / Role>`
Description:
`<Who is the user and what does he need?>`

Main needs:
- `<Need 1>`
- `<Need 2>`

---

## 6. User Flows

### Flow 1: `<Flow name>`

1. User starts at `<entry point>`.
2. User performs `<action>`.
3. System validates `<validation>`.
4. System returns/shows `<result>`.
5. User can `<next action>`.

Acceptance notes:
- `<What must be true for this flow to work?>`

### Flow 2: `<Flow name>`

1. `<Step 1>`
2. `<Step 2>`
3. `<Step 3>`

---

## 7. Functional Requirements

| ID | Requirement | Priority | Notes |
|---|---|---|---|
| FR-001 | `<The system shall...>` | Must | `<Notes>` |
| FR-002 | `<The system shall...>` | Should | `<Notes>` |
| FR-003 | `<The system shall...>` | Could | `<Notes>` |

Priority scale:
- Must: required for MVP.
- Should: important but can be delayed.
- Could: nice-to-have.
- Won’t: explicitly out of scope for now.

---

## 8. Non-Functional Requirements

| Category | Requirement |
|---|---|
| Performance | `<Expected latency / throughput>` |
| Reliability | `<Expected uptime / retry behavior>` |
| Security | `<Auth, permissions, sensitive data>` |
| Observability | `<Logs, metrics, traces, audit>` |
| Maintainability | `<Code quality, modularity, tests>` |
| Scalability | `<Expected future scale>` |
| Usability | `<UX expectations>` |
| Accessibility | `<If relevant>` |

---

## 9. Constraints

Technical constraints:
- `<Stack constraint>`
- `<Infrastructure constraint>`
- `<Integration constraint>`

Business constraints:
- `<Timeline>`
- `<Budget>`
- `<Operational constraint>`

Team constraints:
- `<Team knowledge>`
- `<Maintenance ownership>`

---

## 10. Risks

| Risk | Impact | Probability | Mitigation |
|---|---|---|---|
| `<Risk 1>` | High/Medium/Low | High/Medium/Low | `<Mitigation>` |
| `<Risk 2>` | High/Medium/Low | High/Medium/Low | `<Mitigation>` |

---

## 11. Open Questions

| ID | Question | Owner | Status |
|---|---|---|---|
| Q-001 | `<Question>` | `<Owner>` | Open |
| Q-002 | `<Question>` | `<Owner>` | Open |

---

## 12. Acceptance Criteria

The MVP is accepted when:

- [ ] `<Critical capability works>`
- [ ] `<Main user flow completed>`
- [ ] `<Validation/errors handled>`
- [ ] `<Tests pass>`
- [ ] `<Documentation updated>`
- [ ] `<Security requirements met>`
- [ ] `<Deployment/run instructions verified>`

---

## 13. Out-of-Scope Future Ideas

- `<Future idea 1>`
- `<Future idea 2>`
- `<Future idea 3>`
